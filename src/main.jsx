import React from "react";
import ReactDOM from "react-dom/client";
import SpellCheckApp from "./App";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SpellCheckApp />
  </React.StrictMode>
);
